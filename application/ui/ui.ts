import { iUI } from "./iUI";

export class UI implements iUI {
    protected webixUI: any; 
    
    renderUI(): void {
        
    }   
    event(e: string): void {
        
    }
    getWebixUI() {
        
    }
}